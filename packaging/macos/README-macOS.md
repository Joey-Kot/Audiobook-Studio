# macOS Runtime

Audiobook-Studio uses the system WebKit framework on macOS. A separate WebView runtime is normally not required.

If macOS blocks the app because it is downloaded from the internet, open System Settings, go to Privacy & Security, and allow the app there.

